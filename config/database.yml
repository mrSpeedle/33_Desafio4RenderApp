# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: desafio4
  username: postgres
  password: Kronos92
  host: localhost
  port: 5432

test:
  <<: *default
  database: desafio4_test
  username: postgres
  password: Kronos92
  host: localhost

production:
  <<: *default
  database: desafio4_production
  username: postgres
  password: <%= ENV["desafio4_DATABASE_PASSWORD"] %>
